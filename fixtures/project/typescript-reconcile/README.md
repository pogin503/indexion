# TypeScript Reconcile Fixture

This fixture documents the public API exposed by `src/api.ts`.

Use `buildGraph` with `ReconcileConfig` to assemble a graph.
`ReportRenderer` formats the final output for review.
