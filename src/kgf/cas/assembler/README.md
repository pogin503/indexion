# assembler

## API

- **`is_complete`** (Function) — Check if the assembly was successful (no missing chunks).
- **`all_resolved`** (Function) — Check if all references were resolved.
- **`empty`** (Function) — Create an empty assembly result.
- **`AssemblyResult`** (Struct) — Result of graph assembly.

And 3 more symbols.
