# vcs

## API

- **`detect`** (Function) — Detect which VCS controls `start_dir` by walking up to the filesystem root.
- **`hooks_dir`** (Function) — Return the hooks directory for a known VCS root.
- **`VCS`** (Enum) — Supported version control systems.
