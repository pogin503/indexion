# search

## API

- **`inline_search`** (Function) — Search documents by natural language query using TF-IDF cosine similarity.
- **`SearchDocument`** (Struct) — A document in the search corpus. Domain-agnostic — any module that needs
- **`SearchEngine`** (Struct) — Search engine with vcdb HNSW backend.
