# embed

## API

- **`build_chat_request_json`** (Function) — Build chat completion request JSON using @json library.
- **`default`** (Function) — Default config for text-embedding-3-small (1536 dims)
- **`parse_impression_response`** (Function) — Parse LLM response to Impression
- **`parse_embedding_response`** (Function) — Parse embedding response JSON
- **`OpenAIConfig`** (Struct) — OpenAI API configuration
- **`build_request_body`** (Function) — Build request body JSON
- **`escape_json`** (Function) — Escape string for JSON

And 23 more symbols.
