# hash

## API

- **`compute_source_hash`** (Function) — Compute hash of function source content.
- **`combine_hashes`** (Function) — Combine multiple hashes into one (for manifest or batch operations).
- **`is_valid_hash`** (Function) — Validate that a string looks like a valid FunctionHash.

And 5 more symbols.
