# traverse

## API

- **`TraversalResult`** (Struct) — Result of computing traversal order.
- **`at_depth`** (Function) — Get functions at a specific depth.
- **`new`** (Function) — Create an empty TraversalResult.
- **`max_depth`** (Function) — Get the maximum depth.

And 5 more symbols.
