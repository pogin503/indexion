# segmentation

## API

- **`to_text_segments`** (Function) — Convert segment points to text segments.
- **`default_punctuation_config`** (Function) — Create default punctuation config.
- **`default_tfidf_config`** (Function) — Create default TF-IDF config.
- **`default_window_config`** (Function) — Create default window config.

And 6 more symbols.
