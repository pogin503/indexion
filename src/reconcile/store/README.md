# store

## API

- **`ensure_cache_dir`** (Function) — Ensures that `cache_dir` exists and is a directory.
- **`read_json_file`** (Function) — Reads and parses a JSON file at `path`. Returns `Ok(json)` on success,
- **`write_json_file`** (Function) — Writes JSON to `path`. Returns `Ok(())` on success,
- **`cache_path`** (Function) —
