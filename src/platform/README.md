# platform

## API

- **`detect_arch`** (Function) — Detect the CPU architecture.
- **`detect_platform`** (Function) — Detect the current operating system platform.
- **`path_join`** (Function) — Join two path segments with forward slash.
- **`Platform`** (Enum) — Detected operating system platform.
- **`Arch`** (Enum) — Detected CPU architecture.
- **`get_home_dir`** (Function) — Get user home directory.

And 34 more symbols.
