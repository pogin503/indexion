# template

## API

- **`parse_issue_form`** (Function) — Parse an issue form template from YAML string.
- **`load_issue_form`** (Function) — Load and parse issue form template from file.
- **`MarkdownField`** (Struct) — Markdown content (static, not user-editable).
- **`FilledIssueForm`** (Struct) — Generate filled issue form output with title.
- **`IssueFormTemplate`** (Struct) — GitHub Issue Form template (YAML format).
- **`FormField`** (Enum) — A field in the issue form.
- **`InputField`** (Struct) — Single-line text input.
- **`CheckboxOption`** (Struct) — Single checkbox option.
- **`TextareaField`** (Struct) — Multi-line text input.

And 46 more symbols.
