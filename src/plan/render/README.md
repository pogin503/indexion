# render

## API

- **`format`** (Function) — Format GitHubIssue as a full Markdown document with title, labels, and body.
- **`GitHubIssue`** (Struct) — GitHub Issue output format.
- **`RenderStyle`** (Struct) — Rendering style options.
- **`markdown`** (Function) — Default markdown style.

And 10 more symbols.
