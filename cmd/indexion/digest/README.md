# digest

## API

- **`ensure_dir_exists`** (Function) — Ensure directory exists, creating parent directories as needed.
- **`log_progress`** (Function) — Write progress message (TODO: use stderr when available).
- **`log_error`** (Function) — Write error message (TODO: use stderr when available).
- **`resolve_provider`** (Function) — Resolve embedding provider from CLI config.
- **`build_digest_config`** (Function) — Build DigestConfig from CLI config.
- **`build_graph_from_source`** (Function) — Build graph from source directory.
- **`default`** (Function) — Create default configuration.
- **`load_graph_from_file`** (Function) — Load graph from JSON file.
- **`run_stats`** (Function) — Show index statistics.

And 19 more symbols.
