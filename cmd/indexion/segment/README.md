# segment

## API

- **`run_with_config`** (Function) — Run the segment command with a parsed config.
- **`command`** (Function) — Define the segment command for argparse.
- **`pad_number`** (Function) — Pad number with leading zeros.

And 18 more symbols.
