# common

## API

- **`parse_double`** (Function) — Parse a double from string.
- **`parse_comma_list`** (Function) — Parse a comma-separated list, trimming surrounding spaces per entry.
- **`trim_ascii_spaces`** (Function) — Strip leading and trailing ASCII whitespace from a string.
- **`parse_int`** (Function) — Parse an integer from string.

And 5 more symbols.
